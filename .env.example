# 这是 顶级占位：具体配置看 extension/.env.example 和 api/.env.example
# 真实 key 不进 git，仅本地 / Vercel 控制台保存
EOF

ls -la /home/shikeqiang/videosummary/
echo "---"
find /home/shikeqiang/videosummary -type f -not -path "*/node_modules/*" -not -path "*/.git/*" -not -path "*/.next/*" -not -path "*/.plasmo/*" -not -path "*/build/*" | sort
